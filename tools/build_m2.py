"""Сборка ноутбука КИМ 2.1 — Backprop и обучение сети (Модуль 2)."""
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent))
from nb_builder import Notebook, md, code, placeholder

nb = Notebook("КИМ 2.1 — Backprop и обучение")

nb.add(md("""# КИМ 2.1. Backprop и обучение сети

**Модуль 2. Обучение искусственных нейронных сетей** · Курс «Основы нейронных сетей» · УрФУ

Проверяемый результат (КРМ v3.0):
- **DL-1.1 (Б):** реализует backprop, выбирает активацию, связность и
  инициализацию;
- **вклад M2 в составной DL-1.1 (С):** выбирает loss и `batch_size`, применяет
  регуляризацию и Dropout, объясняет градиентный спуск.

M2 не подтверждает С самостоятельно: полный уровень С требует одновременно
`шлюз M2 = 1` и `шлюз M7 = 1`; шлюз M7 проверяет выбор скорости обучения (`lr`).
Все части А–В этого КИМ обязательны.

Подробное описание, критерии и контрольные вопросы — в `kim-01-backprop-training.md`."""))

# === Часть А. Backprop на NumPy ===
nb.add(md("""---
## Часть А. Backprop на чистом NumPy (обязательно)

Цель — увидеть, как градиенты реально текут через вычислительный граф. Реализуем
двухслойную сеть $784 \\to 64 \\to 10$ и алгоритм обратного распространения ошибки
**вручную**, через правило цепи.

Используем row-major конвенцию: объекты находятся в строках
$X \\in \\mathbb{R}^{B \\times 784}$, веса имеют формы
$W_1 \\in \\mathbb{R}^{784 \\times 64}$ и
$W_2 \\in \\mathbb{R}^{64 \\times 10}$. Архитектура:
$$z_1 = X W_1 + b_1, \\quad a_1 = \\mathrm{ReLU}(z_1), \\quad
z_2 = a_1 W_2 + b_2, \\quad \\hat{y} = \\mathrm{softmax}(z_2).$$

Функция потерь — **mean cross-entropy** по пакету размера $B$:
$$L = -\\frac{1}{B}\\sum_{i=1}^{B}\\sum_{k=1}^{10}
y_{ik}\\log \\hat{y}_{ik}.$$"""))

nb.add(md("""Шаг градиентного спуска для любого параметра:
$$\\theta \\leftarrow \\theta - \\mathrm{lr}\\,\\nabla_\\theta L.$$

**Full-batch GD** использует весь train-набор для одного обновления за эпоху,
**stochastic GD** — один объект на обновление, **mini-batch GD** — пакет объектов
и выполняет $\\lceil N/B \\rceil$ обновлений за эпоху. Объясните различия в шуме
градиента, памяти и числе обновлений. Скорость `lr` в M2 фиксируется; её выбор
отдельно подтверждается в M7."""))

nb.add(md("### 0. Импорт библиотек и загрузка подмножества Fashion-MNIST"))
nb.add(placeholder())

nb.add(md("""Загрузите Fashion-MNIST и возьмите подмножество (например, 10 000 примеров
для скорости): преобразуйте в вектор длины 784, нормируйте в $[0, 1]$.
`one-hot` для меток обязателен — будем использовать кросс-энтропию напрямую."""))

nb.add(md("""### 1. Инициализация параметров

Инициализируйте `W1` (784×64), `b1` (64), `W2` (64×10), `b2` (10) малыми
случайными значениями. Зафиксируйте `np.random.seed(42)`."""))
nb.add(placeholder("W1, b1, W2, b2"))

nb.add(md("""### 2. Прямой проход

Реализуйте `forward(X, W1, b1, W2, b2)` → `(z1, a1, z2, y_hat)`:
`z1 = X @ W1 + b1`, `a1 = relu(z1)`, `z2 = a1 @ W2 + b2`. Не забудьте
`softmax` по оси классов на выходе."""))
nb.add(placeholder("forward"))

nb.add(md("""### 3. Функция потерь

Реализуйте `loss(y_hat, y)` как mean cross-entropy: сумму по объектам и классам
разделите на размер пакета $B$. Добавьте малый $\\varepsilon$ в `log` во
избежание $\\log 0$."""))
nb.add(placeholder("loss"))

nb.add(md("""### 4. Обратный проход (backprop)

Реализуйте `backward(X, y, z1, a1, y_hat, W2)` →
`(dW1, db1, dW2, db2)`. Матрица `W2` обязательна для вычисления `dA1`.

Подсказка — для mean cross-entropy обозначим градиент по логитам как `dy`:
$$dy = \\frac{\\partial L}{\\partial z_2} = \\frac{\\hat{y} - y}{B}, \\quad
dW_2 = a_1^T dy, \\quad db_2 = \\sum_i dy_i,$$
$$dA_1 = dy W_2^T, \\quad dz_1 = dA_1 \\odot \\mathbb{1}[z_1 > 0],$$
$$dW_1 = X^T dz_1, \\quad db_1 = \\sum_i dz_{1,i}.$$"""))
nb.add(placeholder("backward(X, y, z1, a1, y_hat, W2)"))

nb.add(md("""### 4.1. Численная проверка градиента (обязательно)

Выберите хотя бы один элемент матрицы весов и сравните аналитический градиент с
центральной разностью
$$g_{num} = \\frac{L(\\theta + \\varepsilon)-L(\\theta-\\varepsilon)}{2\\varepsilon}.$$
Выведите оба значения и относительную ошибку; после проверки восстановите
исходный параметр."""))
nb.add(placeholder("gradient check хотя бы одного параметра"))

nb.add(md("""### 5. Цикл обучения

Реализуйте цикл по эпохам (50–100), внутри — мини-батчи (например, по 64 примера):
прямой проход → loss → backward → обновление `W -= lr * dW`."""))
nb.add(placeholder("training loop, lr=0.1"))

nb.add(md("""### 6. Проверка общего тренда обучения

Посчитайте и выведите loss и accuracy на train после каждой эпохи. Постройте
графики. Из-за шума mini-batches отдельные эпохи могут ухудшаться; требуется
общий тренд снижения loss и роста accuracy, а не монотонность."""))
nb.add(placeholder("графики loss и accuracy"))

nb.add(md("""**Контрольный вопрос:** на чём основан ваш backprop? Сформулируйте
правило цепи и покажите, как оно применяется к $\\partial L / \\partial W_1$."""))
nb.add(md("> *Ваш ответ:* ..."))

# === Часть Б. Цикл обучения на фреймворке ===
nb.add(md("""---
## Часть Б. Цикл обучения на фреймворке (обязательно)

### 7. Выбор функции потерь и та же сеть на фреймворке

Реализуйте оба контекста:

| Задача | Выход модели (сырые logits) | Targets | Loss |
|---|---|---|---|
| Fashion-MNIST, 10 взаимоисключающих классов | `(B, 10)` | индексы `(B,)`, integer/`long` | `CrossEntropyLoss` |
| Fashion-MNIST, только классы 0/1 | `(B,)` или `(B, 1)` | 0/1 той же формы, float | `BCEWithLogitsLoss` |

Для multiclass не добавляйте `Softmax` перед CE, для binary — `Sigmoid` перед
BCEWithLogits. Обучите обе модели и покажите validation loss и accuracy.
Объясните, почему отклонены: BCEWithLogits с индексами классов для данной
однозначной multiclass-задачи; CE с одним binary-logit и float-target (CE
потребовал бы два logits и целевой индекс); MSE как основной classification loss.

### 7.1. Многоклассовая модель

Перепишите двухслойную сеть на PyTorch и обучите явным циклом с
`optimizer.zero_grad()`, `forward`, `loss.backward()`, `optimizer.step()`."""))
nb.add(placeholder("multiclass: CrossEntropyLoss"))

nb.add(md("""### 7.2. Бинарная модель для классов 0/1

Отфильтруйте классы 0 и 1 отдельно в train и validation, создайте модель с одним
выходным logit и реально обучите её с `BCEWithLogitsLoss`. Для accuracy примените
порог 0 к logits (это соответствует вероятности 0.5 после sigmoid)."""))
nb.add(placeholder("binary: BCEWithLogitsLoss"))

nb.add(md("""### 8. Выбор размера пакета

До запуска зафиксируйте правило: максимизировать validation accuracy; среди
вариантов не хуже максимума более чем на 0.002 выбрать вариант с минимальным
временем обучения. Test-набор не загружать и не использовать до завершения всех
выборов.

Запустите одинаковое число эпох с `batch_size` 10, 50, 200, 500. Представьте
таблицу: `batch_size`, validation loss, validation accuracy, полное время и/или
время эпохи, обновлений за эпоху и всего. По предзаданному правилу программно
выберите итоговый `batch_size`, затем объясните различия full-batch,
mini-batch и stochastic GD."""))
nb.add(placeholder("таблица batch_size + итоговый выбор без test leakage"))

nb.add(md("""### 9. Выбор активации, инициализации и связности

В едином validation-протоколе сравните четыре конфигурации:
- один плотный hidden layer, `ReLU` и инициализация He;
- тот же hidden layer, `ReLU + He`, но с фиксированной воспроизводимой бинарной
  маской, оставляющей ровно 50% связей `input → hidden` активными;
- один плотный hidden layer, `tanh` и инициализация Xavier;
- два плотных hidden layers, `ReLU` и инициализация He.

Маска должна применяться к весам при каждом forward, не обучаться и оставаться
одинаковой во всех эпохах. Зафиксируйте seed маски, общий seed, число эпох,
optimizer и выбранный в части 8 batch size. Сведите validation loss/accuracy,
время, число параметров и фактическую долю активных связей первого hidden layer
в таблицу. По validation accuracy отдельно выберите плотную или разреженную
связность в контролируемой паре `ReLU + He, 1 layer`, затем выберите общую
конфигурацию и обоснуйте активацию, инициализацию, глубину и связность."""))
nb.add(placeholder("четыре конфигурации, fixed mask 50%, таблица и выбор"))

# === Часть В. Переобучение и регуляризация ===
nb.add(md("""---
## Часть В. Переобучение и регуляризация (обязательно)

### 10. Обнаружение переобучения

Увеличьте число эпох до 100–200. Постройте на одном графике `loss`/`val_loss` и
`accuracy`/`val_accuracy`. Найдите эпоху, после которой `val_loss` начинает расти
(переобучение)."""))
nb.add(placeholder("кривые train/val"))

nb.add(md("""### 11. Регуляризация

В одном протоколе обязательно сравните baseline без регуляризации, `Dropout` и
`L2`; сохраните `EarlyStopping` с восстановлением лучших весов. Для одного
регуляризатора сравните не менее двух значений силы (например, `Dropout(0.2)` и
`Dropout(0.4)`) и выберите одно по validation loss. Сведите validation loss,
accuracy и разрыв train/validation в таблицы. Улучшение не предполагайте заранее,
test для настройки не используйте."""))
nb.add(placeholder("baseline / два Dropout / L2 / EarlyStopping + выбор"))

nb.add(md("""### 12. Вывод

Какие методы регуляризации оказались эффективнее и почему? Запишите краткий вывод."""))
nb.add(md("> *Ваш вывод:* ..."))

nb.add(md("""---
## Итог

Перед сдачей убедитесь, что:
- [ ] выполнены все части А–В;
- [ ] backprop на NumPy работает (общий тренд: loss снижается, accuracy растёт);
- [ ] записана формула GD, объяснены три режима и выполнен gradient check;
- [ ] реально обучены multiclass CE и binary BCEWithLogits, объяснены форматы и
      отклонённые альтернативы;
- [ ] для пакетов 10/50/200/500 есть validation-таблица времени и обновлений,
      предзаданное правило и итоговый выбор без test leakage;
- [ ] сравнены активации, инициализации, один/два hidden layers и контролируемая
      пара плотной/фиксированной 50%-разреженной связности;
- [ ] построены графики кривых обучения;
- [ ] baseline, Dropout, L2 и EarlyStopping сравнены, две силы регуляризатора
      проверены и одна выбрана по validation;
- [ ] вы готовы вывести градиенты по слоям на защите.

**Артефакты сдачи:** заполненный ноутбук; код backprop и gradient check; результаты
обоих loss-контекстов; таблицы выбора `batch_size` и архитектуры, включая долю
активных связей и dense/sparse-выбор; графики
train/validation; таблицы регуляризации; выводы; устная защита.

**Формат сдачи:** заполненный ноутбук + устная защита."""))

path = "M2-training/attachments/kim-02-backprop-training.ipynb"
nb.save(path)
print(f"Сохранён: {path}  ({nb.cell_count()} ячеек)")
