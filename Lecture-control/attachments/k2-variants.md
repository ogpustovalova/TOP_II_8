# Рубежный срез К2: варианты

Каждый вариант содержит четыре коротких вопроса, две расчётные задачи и одну
ситуационную задачу. Все промежуточные вычисления должны быть показаны.

## Вариант 1

### Часть А. Термины и концепции

1. Как residual connection влияет на прохождение сигнала и градиента?
2. Что делает max pooling и какую информацию он может потерять?
3. Чем состояние LSTM отличается от состояния GRU?
4. Зачем в функции потерь VAE используется KL-дивергенция?

### Часть Б. Расчётные задачи

1. На вход `32×32×3` подаётся `Conv2D` с 32 фильтрами `3×3`, `stride=1`,
   `padding=1`, затем `MaxPool 2×2`, `stride=2`. Найдите пространственный размер
   после каждого слоя и число параметров свёртки со смещениями.
2. Для абстрактного LSTM с одним вектором смещения на каждый гейт вычислите
   число параметров при `input_size=64`, `hidden_size=32`. Используйте четыре
   гейта.

### Часть В. Ситуационная задача

Для обучения классификатора CIFAR-10 доступны только 20 изображений каждого
класса; отдельные фиксированные validation и test уже подготовлены.
Спроектируйте честное сравнение frozen backbone и fine-tuning: преобразования,
learning rate, метрики и критерий выбора. Объясните основные риски и ожидаемый
результат.

## Вариант 2

### Часть А. Термины и концепции

1. Почему свёртка использует меньше параметров, чем полносвязный слой над тем же
   изображением?
2. Зачем при transfer learning сначала замораживают backbone, а при последующем
   fine-tuning уменьшают learning rate?
3. Чем токенизация, морфологический и синтаксический анализ отличаются друг от
   друга?
4. Чем AdamW отличается от Adam с L2-слагаемым в градиенте?

### Часть Б. Расчётные задачи

1. На вход `64×64×3` подаётся свёртка с 16 фильтрами `5×5`, `stride=2`,
   `padding=2`. Найдите размер выхода и число параметров со смещениями.
2. Для абстрактной GRU с одним вектором смещения на каждый гейт вычислите число
   параметров при `input_size=50`, `hidden_size=64`. Используйте три гейта.

### Часть В. Ситуационная задача

LSTM-классификатор отзывов показывает 98 % accuracy на train и 62 % на
validation. Словарь был построен до разбиения данных. Поставьте диагноз,
предложите корректный протокол подготовки текста и сравнения LSTM, GRU и 1D-CNN.

## Вариант 3

### Часть А. Термины и концепции

1. Чем различаются regex-нормализация, морфологический и синтаксический анализ
   текста? Какой артефакт возвращает каждый этап?
2. Зачем VAE использует репараметризацию `z=μ+σ·ε`?
3. Для чего нужен scheduler скорости обучения?
4. Зачем проверять модель после сохранения и повторной загрузки?

### Часть Б. Расчётные задачи

1. На вход `28×28×1` подаётся свёртка с 32 фильтрами `3×3`, `stride=2`,
   `padding=1`. Найдите размер выхода и число параметров со смещениями.
2. Энкодер VAE имеет общий слой `10 → 8` и две головы `8 → 2` для `μ` и
   `log σ²`; все слои имеют смещения. Рассчитайте число параметров. Затем для
   `μ=[0,1]`, `log σ²=[0,0]` вычислите
   `D_KL=-0,5 Σ(1+log σ²-μ²-exp(log σ²))`.

### Часть В. Ситуационная задача

Модель прогнозирования температуры обучена на случайно перемешанных окнах всего
временного ряда и значительно превосходит baseline. Объясните, почему результат
может быть недостоверным. Предложите корректное разбиение, нормализацию,
формирование окон и процедуру сравнения с baseline. Добавьте честный протокол
сравнения LSTM и GRU с `hidden_size=32/64`.

## Вариант 4

### Часть А. Термины и концепции

1. Чем self-attention отличается от рекуррентного переноса скрытого состояния
   в RNN по доступу к прошлым токенам?
2. Зачем в scaled dot-product attention делят на \(\sqrt{d_k}\)?
3. Что такое feature extraction и fine-tuning при transfer learning?
4. Чем `ReduceLROnPlateau` отличается от `CosineAnnealingLR` по сигналу смены
   learning rate?

### Часть Б. Расчётные задачи

1. На вход `16×16×8` подаётся `Conv2D` с 16 фильтрами `3×3`, `stride=1`,
   `padding=1`, затем `MaxPool 2×2`, `stride=2`. Найдите пространственный размер
   после каждого слоя и число параметров свёртки со смещениями.
2. Для одного head scaled dot-product attention при длине последовательности
   \(T=4\) и \(d_k=d_v=2\) матрицы \(Q,K,V\) имеют размер \(T×d_k\). Сколько
   скалярных значений содержит матрица attention-весов после softmax
   (без multi-head)?

### Часть В. Ситуационная задача

Команда сравнивает LSTM и предобученный DistilBERT на классификации коротких
отзывов. LSTM обучают с нуля на своей подвыборке; DistilBERT берут «из коробки»
без fine-tuning и смотрят accuracy на test, который уже использовали для выбора
гиперпараметров LSTM. Укажите не менее трёх нарушений корректного протокола и
предложите честную схему сравнения (split, что замораживается, когда смотрят
test, какие метрики кроме accuracy).

## Вариант 5

### Часть А. Термины и концепции

1. Зачем в CNN используют несколько фильтров на одном слое?
2. Что такое receptive field и как его увеличивают?
3. Чем teacher forcing в языковых моделях отличается от свободной генерации?
4. Зачем в Adam хранят оценки первого и второго момента градиента?

### Часть Б. Расчётные задачи

1. Вход `20×20×16`, свёртка 8 фильтров `3×3`, stride=1, padding=0. Размер
   выхода и число параметров со смещениями.
2. LSTM: `input_size=10`, `hidden_size=20`, один bias на гейт, 4 гейта — число
   параметров.

### Часть В. Ситуационная задача

YOLO-детектор хорошо работает на дневных кадрах из train и плохо ночью. Какие
гипотезы и как спланировать сбор данных / аугментацию / дообучение, не портя
дневное качество?

## Вариант 6

### Часть А. Термины и концепции

1. Чем global average pooling отличается от flatten перед классификатором?
2. Зачем в ResNet bottleneck-блок `1×1 → 3×3 → 1×1`?
3. Что делает embedding-слой и откуда берутся его веса в начале обучения?
4. Чем weight decay отличается от уменьшения learning rate?

### Часть Б. Расчётные задачи

1. Вход `32×32×1`, Conv 16×`5×5`, stride=1, padding=2; затем pool 2×2 stride 2.
   Размеры и параметры свёртки.
2. GRU: `input_size=32`, `hidden_size=16`, один bias на гейт, 3 гейта — параметры.

### Часть В. Ситуационная задача

Fine-tuning ResNet с большим lr «забыл» ImageNet-признаки (accuracy ниже
frozen baseline). Диагноз и протокол safe fine-tuning.

## Вариант 7

### Часть А. Термины и концепции

1. Чем dilated convolution отличается от обычной?
2. Зачем pack_padded_sequence в RNN?
3. Что измеряет reconstruction loss автокодировщика?
4. Когда предпочитают RMSProp / Adam вместо SGD с momentum?

### Часть Б. Расчётные задачи

1. Вход `14×14×32`, Conv 32×`3×3`, stride=2, padding=1 — размер и параметры.
2. VAE: слой `μ` и `log σ` по `16 → 4` со смещениями (две головы). Параметры
   голов. При `μ=0`, `log σ²=0` вклад KL для одной координаты.

### Часть В. Ситуационная задача

AE отлично восстанавливает train и validation, но не ловит аномалии в проде.
Возможные причины и как перестроить порог и набор аномалий для проверки.

## Вариант 8

### Часть А. Термины и концепции

1. Чем multi-head attention лучше одного head при той же суммарной размерности?
2. Зачем positional encoding в трансформере?
3. Чем batch norm отличается от layer norm по осям статистики?
4. Что такое warm-up learning rate?

### Часть Б. Расчётные задачи

1. Вход `8×8×64`, Conv 64×`3×3`, padding=1, stride=1 — размер и параметры.
2. Attention: `T=8`, `d_k=16` — форма матрицы `QK^T` до softmax для одного head.

### Часть В. Ситуационная задача

Команда утверждает, что «трансформер всегда лучше LSTM» на их 500 размеченных
отзывах. Какие условия эксперимента обязательны, чтобы утверждение было
проверяемым, а не лозунгом?

## Вариант 9

### Часть А. Термины и концепции

1. Чем skip connection отличается от attention по способу передачи сигнала?
2. Зачем gradient checkpointing?
3. Что такое latent space в AE/VAE?
4. Чем cosine annealing с restarts может помочь по сравнению с одним циклом?

### Часть Б. Расчётные задачи

1. Вход `64×64×3`, Conv 32×`3×3`, stride=2, padding=1 — размер и параметры.
2. Embedding: словарь 5000, dim 64 — число параметров (без bias).

### Часть В. Ситуационная задача

Модель временного ряда с LSTM переобучается на недельную сезонность train и
ломается при смене сезона. Протокол валидации и признаки, которые стоит добавить
или убрать.

## Вариант 10

### Часть А. Термины и концепции

1. Чем encoder-decoder attention (cross-attention) отличается от self-attention?
2. Зачем в детекции non-maximum suppression?
3. Что такое mode collapse в генеративных моделях (кратко)?
4. Почему сравнивать оптимизаторы нужно при индивидуально подобранном initial lr?

### Часть Б. Расчётные задачи

1. Вход `28×28×3`, две свёртки подряд: 16×`3×3` padding=1, затем 32×`3×3`
   padding=1, stride=1. Размер после второй и параметры второй свёртки.
2. Multi-head: 4 head, `d_model=64` — `d_k` на head при равномерном разделении.

### Часть В. Ситуационная задача

Нужно выбрать между 1D-CNN, BiLSTM и frozen RuBERT для модерации комментариев
(latency < 50 мс на CPU). Спроектируйте критерии выбора: качество, скорость,
память, стоимость разметки и риск деградации.
