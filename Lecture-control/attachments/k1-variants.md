# Рубежный срез К1: варианты

Каждый вариант содержит четыре коротких вопроса, две расчётные задачи и одну
ситуационную задачу. Все промежуточные вычисления должны быть показаны.

## Вариант 1

### Часть А. Термины и концепции

1. Почему композиция нескольких линейных слоёв без нелинейной активации
   эквивалентна одному линейному преобразованию?
2. Почему в PyTorch перед `CrossEntropyLoss` обычно не применяют `Softmax`?
3. Зачем вызывать `optimizer.zero_grad()` перед обратным проходом?
4. Какое практическое ограничение имеет универсальная теорема аппроксимации?

### Часть Б. Расчётные задачи

1. Рассчитайте число обучаемых параметров MLP `784 → 64 → 10`. У обоих слоёв
   есть смещения.
2. Для модели `ŷ = wx + b` даны `x=2`, `y=0`, `w=0,5`, `b=0` и
   `L=0,5(ŷ-y)²`. Выполните один шаг градиентного спуска с `lr=0,1`: найдите
   `L`, градиенты по `w` и `b`, новые значения параметров.

### Часть В. Ситуационная задача

MLP достигает 99 % accuracy на train, 68 % на validation; train loss продолжает
снижаться, а validation loss после 12-й эпохи растёт. Поставьте диагноз,
предложите порядок проверок и не менее трёх изменений протокола. Объясните,
какие графики и метрики подтвердят улучшение.

## Вариант 2

### Часть А. Термины и концепции

1. Чем насыщение sigmoid отличается от поведения ReLU при положительных входах?
2. Почему He обычно используют с ReLU, а Xavier — с tanh?
3. В чём компромисс mini-batch SGD между полным batch и обновлением по одному
   объекту?
4. Почему параметры нормализации нельзя вычислять по train и test вместе?

### Часть Б. Расчётные задачи

1. Рассчитайте число обучаемых параметров MLP `8 → 64 → 32 → 1`, включая
   смещения.
2. Для логитов двух классов `z=[ln 2, 0]` вычислите softmax-вероятности и
   cross-entropy, если истинный класс — второй.

### Часть В. Ситуационная задача

Исследователь сначала нормализовал весь датасет, затем случайно разделил его на
train, validation и test. Полученная test-ошибка заметно ниже результата после
развёртывания. Объясните источник и направление смещения оценки. Предложите
корректный конвейер подготовки данных и проверки модели. Назовите ещё две
возможные причины разрыва между test и эксплуатацией и объясните, как отличить
их экспериментально.

## Вариант 3

### Часть А. Термины и концепции

1. Зачем MLP нужна нелинейность между полносвязными слоями?
2. Чем должен отличаться выходной слой для регрессии и многоклассовой
   классификации?
3. Почему Dropout ведёт себя по-разному в режимах обучения и инференса?
4. Как слишком большая и слишком малая скорость обучения проявляются на кривой
   loss?

### Часть Б. Расчётные задачи

1. Рассчитайте число обучаемых параметров MLP `784 → 128 → 64 → 10`, включая
   смещения.
2. Для бинарного нейрона `z=wx+b`, `p=sigmoid(z)` даны `x=2`, `y=1`, `w=0`,
   `b=0`. Для binary cross-entropy используйте
   `∂L/∂z = p-y`. Выполните один шаг с `lr=0,1` и найдите новые `w` и `b`.

### Часть В. Ситуационная задача

При обучении MLP loss через несколько шагов становится `NaN`. Сформулируйте не
менее четырёх проверяемых гипотез, расположите проверки в разумном порядке и
предложите исправления. Укажите, какие диагностические значения нужно
логировать.

## Вариант 4

### Часть А. Термины и концепции

1. Чем L2-регуляризация отличается от Dropout по механизму и по влиянию на
   веса на этапе инференса?
2. Зачем нужен validation set, если уже есть test?
3. Что означает «затухающий градиент» при обучении глубокой сети и как его
   обнаруживают по нормам градиентов?
4. Почему early stopping смотрят по validation, а не по train loss?

### Часть Б. Расчётные задачи

1. Рассчитайте число обучаемых параметров MLP `100 → 50 → 20 → 5`, включая
   смещения.
2. Для модели `ŷ = wx + b` даны `x=3`, `y=1`, `w=0`, `b=1` и
   `L=0,5(ŷ-y)²`. Выполните один шаг градиентного спуска с `lr=0,05`: найдите
   `L`, градиенты по `w` и `b`, новые значения параметров.

### Часть В. Ситуационная задача

На train accuracy растёт до 95 %, validation accuracy колеблется около 70 % и
не улучшается после 8-й эпохи; при этом validation loss сначала падает, затем
выходит на плато. Студент предлагает увеличить число эпох вдвое. Оцените это
предложение, сформулируйте не менее трёх альтернативных действий и укажите, по
каким метрикам и графикам решить, что конфигурация улучшилась, не трогая test
до финальной оценки.

## Вариант 5

### Часть А. Термины и концепции

1. Чем batch gradient descent отличается от SGD по оценке градиента и стоимости
   одной эпохи?
2. Зачем в бинарной классификации часто используют sigmoid на выходе, а не
   линейный слой?
3. Что такое «умершие» ReLU-нейроны и как это проявляется?
4. Почему нельзя подбирать гиперпараметры по test set?

### Часть Б. Расчётные задачи

1. Рассчитайте число обучаемых параметров MLP `32 → 16 → 8 → 2`, включая
   смещения.
2. Для логитов трёх классов `z=[0, ln 2, ln 2]` вычислите softmax-вероятности и
   cross-entropy, если истинный класс — первый.

### Часть В. Ситуационная задача

Модель на validation даёт accuracy 0,91, на test — 0,74. Train accuracy 0,93.
Гиперпараметры подбирали по test «для ускорения». Объясните, что произошло, и
опишите корректный протокол выбора модели и финальной оценки.

## Вариант 6

### Часть А. Термины и концепции

1. Зачем нужен bias (смещение) в линейном слое?
2. Чем MSE отличается от MAE как функция потерь для регрессии?
3. Что делает `model.train()` / `model.eval()` в PyTorch помимо Dropout?
4. Почему инициализация всех весов нулями — плохая идея для скрытых слоёв?

### Часть Б. Расчётные задачи

1. Рассчитайте число обучаемых параметров MLP `64 → 64 → 10` (два слоя со
   смещениями).
2. Для `ŷ = wx + b` даны `x=-1`, `y=1`, `w=1`, `b=0`, `L=0,5(ŷ-y)²`, `lr=0,2`.
   Найдите `L`, градиенты и новые `w`, `b` после одного шага.

### Часть В. Ситуационная задача

После добавления Dropout train accuracy упала, validation почти не изменилась.
Студент считает Dropout «вредным» и отключает его. Оцените рассуждение и
предложите, как правильно интерпретировать разрыв train/validation и выбирать
`p` Dropout.

## Вариант 7

### Часть А. Термины и концепции

1. Что такое softplus и чем он близок к ReLU?
2. Зачем логировать и train, и validation loss на одной оси?
3. Чем one-hot кодирование метки отличается от integer class index в
   `CrossEntropyLoss` PyTorch?
4. Что означает «утёкшая» целевая переменная (target leakage)?

### Часть Б. Расчётные задачи

1. Рассчитайте число параметров слоя `Linear(256, 128)` со смещением и без.
2. Softmax для `z=[1, 1, 1]`: вероятности и cross-entropy при истинном классе 0.

### Часть В. Ситуационная задача

В признаках есть столбец «id клиента», коррелирующий с меткой. Модель даёт
высокий test score в ноутбуке, но в проде бесполезна. Диагноз, проверка и
исправление пайплайна.

## Вариант 8

### Часть А. Термины и концепции

1. Чем momentum в SGD помогает по сравнению с «голым» SGD?
2. Зачем нормализовать входы перед обучением MLP?
3. Что такое capacity модели и как она связана с переобучением?
4. Почему accuracy — плохая единственная метрика при сильном дисбалансе классов?

### Часть Б. Расчётные задачи

1. MLP `10 → 5 → 1` со смещениями: число параметров.
2. `p=sigmoid(0)=0,5`, `y=0`, binary CE: значение `L` и `∂L/∂z = p-y`.

### Часть В. Ситуационная задача

Класс «брак» встречается в 2 % объектов. Модель предсказывает всегда «норма» и
имеет accuracy 98 %. Как перестроить метрики, loss и протокол оценки?

## Вариант 9

### Часть А. Термины и концепции

1. Чем soft-label / label smoothing отличается от hard one-hot?
2. Зачем gradient clipping?
3. Что проверяет learning curve train vs validation?
4. Почему random seed фиксируют при сравнении архитектур?

### Часть Б. Расчётные задачи

1. Параметры MLP `20 → 10 → 10 → 3` со смещениями.
2. Один шаг GD: `ŷ=wx`, `x=4`, `y=2`, `w=1`, `L=0,5(ŷ-y)²`, `lr=0,05` — новые `w`.

### Часть В. Ситуационная задача

Два студента получили разный ranking моделей на одном validation из-за разного
seed и разного порядка батчей. Как стандартизировать протокол сравнения?

## Вариант 10

### Часть А. Термины и концепции

1. Чем L1-регуляризация отличается от L2 по виду решения (разреженность)?
2. Зачем early stopping patience, а не остановка при первом ухудшении?
3. Что такое distribution shift между train и production?
4. Почему «подглядывание» в test при отладке кода опасно даже без явного тюнинга?

### Часть Б. Расчётные задачи

1. Параметры MLP `784 → 32 → 32 → 10` со смещениями.
2. Логиты `z=[0, 0]`, истинный класс 1: softmax и CE.

### Часть В. Ситуационная задача

В проде доля положительного класса выросла с 10 % до 40 %. Старый порог
классификатора даёт много ложных срабатываний. Что пересмотреть в метриках,
пороге и мониторинге, не переобучая модель с нуля в первый день?
