# КИМ 4.1. Сверточные сети и перенос обучения

## Назначение

Лабораторная работа модуля 4 — ключевой КИМ по компьютерному зрению. Студент
знакомится с механикой свёртки, строит собственную CNN для классификации
изображений, использует предобученную ResNet и осваивает перенос обучения
(transfer learning): заморозку backbone и тонкую настройку (fine-tuning) с
пониженной скоростью обучения.

## Проверяемые результаты обучения

> КИУ выбраны по КРМ v3.0. Предметные дескрипторы ниже фиксируют наблюдаемые
> действия этого КИМ; полные редакционно нормализованные формулировки уровней —
> в [`data/krm-descriptors.md`](../data/krm-descriptors.md).

| Компетенция | Индикатор | Уровень | Предметный дескриптор |
|---|---|:---:|---|
| DL-1 | DL-1.3 | Б | Применяет VGG-подобную архитектуру и предобученную ResNet |
| DL-1 | DL-1.4 | Б | Исследует гиперпараметры свертки и пулинга, разрабатывает и обучает CNN |
| DL-1 | DL-1.12 | Б | Применяет перенос обучения, заморозку, fine-tuning и обучение на малой выборке |
| DL-3 | DL-3.1 | Б | Применяет ResNet и YOLO к изображениям и видео, использует OpenCV, сохраняет и загружает модель |

## Условия выполнения

- **Форма:** индивидуальная лабораторная работа.
- **Среда:** Jupyter Notebook (Colab с GPU рекомендуется), Python 3, на выбор —
  TensorFlow/Keras или PyTorch (torchvision).
- **Длительность:** 8 лабораторных часов (≈20 ч с СРС) — самый объёмный модуль, см. [РПД](../docs/rpd.md). ⚠️ _Предложено автором ФОС на основе распределения часов в [РПД](../docs/rpd.md); подлежит согласованию._
- **Данные:** CIFAR-10 для CNN, ResNet и few-shot; предоставленное короткое видео
  или видео, собранное из открытого тестового изображения, для YOLO-пайплайна.

## Содержание задания

Подробно в ноутбуке [`attachments/kim-04-cnn.ipynb`](attachments/kim-04-cnn.ipynb).
Эталонное решение — `attachments/kim-04-cnn-solution.ipynb`.

**Часть А. Свёртка с нуля.**

1. Реализовать операцию двумерной свёртки `(I⋆K)[i,j] = Σ_m Σ_n I[i+m,j+n]·K[m,n]`
   на NumPy для одного канала.
2. Показать влияние размера ядра, шага (stride) и дополнения (padding) на
   размер выхода по формуле
   `H_out = ⌊(H_in − K + 2P)/S⌋ + 1`.

**Часть Б. Собственная CNN на CIFAR-10.**

3. Собрать CNN в стиле VGG: каскады `Conv2D → (BatchNorm) → ReLU → MaxPooling →
   Dropout`, затем `Flatten → Dense → Dense(10, softmax)`.
4. Обучить (`sparse_categorical_crossentropy` / `Adam` / `accuracy`), оценить
   тестовую точность, построить кривые обучения.

**Часть В. Предобученная ResNet и transfer learning.**

5. Загрузить предобученную на ImageNet модель (`ResNet50(weights='imagenet',
   include_top=False')` или `torchvision.models.resnet18(weights=...)`).
6. **Этап 1 — feature extraction:** заморозить backbone (`base_model.trainable = False`),
   добавить новый классификатор (`GlobalAveragePooling2D → Dense → Dense(N, softmax)`),
   обучить только голову.
7. **Этап 2 — fine-tuning:** разморозить backbone, перекомпилировать с низкой
   скоростью обучения (`Adam(1e-5)`), дообучить. Сравнить точность до и после
   fine-tuning.

**Часть Г. Перенос обучения на малой выборке.**

8. Сформировать воспроизводимую сбалансированную выборку по 20 изображений на
   класс CIFAR-10. На одинаковом разбиении сравнить обучение только новой головы
   и fine-tuning, затем сопоставить результат с обучением на полной выборке.

**Часть Д. Готовая модель CV и видеопайплайн.**

9. Загрузить готовую YOLO, выполнить инференс на изображении и коротком видео.
   Кадры читать и записывать через OpenCV, сохранить размеченное видео. Сохранить
   модель в отдельный файл, повторно загрузить её и проверить совпадение классов
   и confidence на контрольном кадре.

## Формат сдачи

- Заполненный ноутбук с выполненными ячейками, графиками и выводами.
- Краткий сравнительный анализ: собственная CNN vs. transfer learning vs.
  transfer learning + fine-tuning (точность, время обучения, число обучаемых
  параметров).
- Таблица few-shot-эксперимента и файлы сохранённой модели и размеченного видео.
- Устная защита.

## Критерии и шкала оценивания

Рубрика: [`rubric-01.md`](rubric-01.md).

## Контрольные вопросы для защиты

- Что вычисляет ядро свёртки? Чем свёртка отличается от полносвязного слоя?
- Как размер ядра, шаг и дополнение влияют на размер карты признаков? Выведите
  формулу размера выхода.
- Зачем нужен слой подвыборки (MaxPooling)? Почему он уменьшает размер в 2 раза
  при пуле 2×2?
- Что такое остаточное соединение (residual connection) в ResNet и какую
  проблему оно решает? Почему «единица в градиенте» гарантирует, что градиент
  не обнулится?
- Зачем нужна пакетная нормализация (BatchNorm)? Почему при ней часто отключают
  смещение (`use_bias=False`)?
- Что такое ImageNet и почему предобучение на нём помогает другим задачам?
- В чём разница между feature extraction (заморозка backbone) и fine-tuning?
  Почему при fine-tuning уменьшают скорость обучения?
- Зачем `include_top=False` и что делает `GlobalAveragePooling2D`?
- Чем обучение на 20 изображениях класса отличается от обычного transfer
  learning? Какие слои разумно замораживать?
- Как устроен цикл чтения и записи видеокадров в OpenCV? Зачем проверять модель
  после сохранения и повторной загрузки?

## Правила использования ресурсов и генеративного ИИ

- Допускается: документация Keras/torchvision, учебник d2l.ai (главы про CNN и
  ResNet), обсуждение концепций с LLM.
- Запрещается: просить LLM собрать архитектуру CNN целиком. Использование
  готовых предобученных моделей разрешено (это часть задания).
- Сдача включает устную защиту с обоснованием архитектурных решений.

## Замечание о материалах курса

В исходных ноутбуках `dlpython_course` эксперимент с разной глубиной сети
(ResNet-20 vs ResNet-56 по He et al. 2015) отсутствует; ResNet представлен
только как готовая предобученная сеть. Полноценная реализация residual-блока и
сравнение глубины — рекомендуемое (но не обязательное для уровня Б) расширение
задания; на уровне Б достаточно обсудить residual connection концептуально и
применить готовую ResNet.

## Источники

- `dlpython_course/02_cnn/tensorflow/cifar10.ipynb`, `02_cnn/tensorflow/fashion_mnist.ipynb`,
  `02_cnn/pytorch/cifar10.ipynb` (собственные CNN).
- `dlpython_course/03_pretrained_networks/tensorflow/resnet_pretrained.ipynb`
  (предобученная ResNet).
- `dlpython_course/04_transfer_learning/tensorflow/scenes.ipynb` (двухэтапный
  transfer learning: заморозка + fine-tuning с `Adam(1e-5)` — образец).
- `dlpython_course/04_transfer_learning/pytorch/animals.ipynb` (PyTorch:
  замена FC-головы).
- Методический ориентир: лекция Аммара Али (08.07.2026) — residual connections
  и проблема затухающего градиента.
