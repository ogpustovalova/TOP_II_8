# КИМ 5.1. Рекуррентные сети и 1D-CNN для последовательностей

## Назначение

Лабораторная работа модуля 5. Студент осваивает два семейства архитектур для
последовательностей — рекуррентные сети (RNN/LSTM/GRU) и одномерные свёрточные
сети (1D-CNN) — и применяет их к двум типам данных: текстам (классификация
тональности отзывов) и временным рядам (прогноз температуры). Особое внимание —
предобработке последовательностей (токенизация, лемматизация, эмбеддинги,
выравнивание длины).

## Проверяемые результаты обучения

> Дескрипторы приведены дословно из КРМ v3.0 (полные формулировки всех трёх
> уровней — в [`data/krm-descriptors.md`](../data/krm-descriptors.md)).

| Компетенция | Индикатор | Уровень | Дескриптор (КРМ v3.0, дословно) |
|---|---|:---:|---|
| DL-1 | DL-1.5 | П | Понимает принципы функционирования и обучения фильтров и ячейки памяти в GRU и LSTM блоках; понимает принцип обучения с помощью обратного распространения по времени |
| DL-4 | DL-4.1 | С | Владеет инструментами грамматического разбора структурированных и слабо-структурированных текстов, способен написать свой парсер. Владеет инструментами разметки текстовых данных и формирования словарей |

## Условия выполнения

- **Форма:** индивидуальная лабораторная работа.
- **Среда:** Jupyter Notebook (Colab с GPU рекомендуется), Python 3,
  TensorFlow/Keras (основной вариант), при желании — PyTorch.
- **Длительность:** 6 лабораторных часов (≈16 ч с СРС) — см. [РПД](../docs/rpd.md). ⚠️ _Предложено автором ФОС на основе распределения часов в [РПД](../docs/rpd.md); подлежит согласованию._
- **Датасеты:** отзывы на банки `banks.csv` (бинарная классификация тональности),
  метеоданные Йены `jena_climate_2009_2016.csv` (регрессия температуры).

## Содержание задания

Подробно в ноутбуке [`attachments/kim-05-sequences.ipynb`](attachments/kim-05-sequences.ipynb).
Эталонное решение — `attachments/kim-05-sequences-solution.ipynb`.

**Часть А. Предобработка текстов.**

1. Токенизация (`nltk.word_tokenize`), лемматизация (`pymorphy3`), удаление
   стоп-слов и пунктуации.
2. Построение словаря по частоте (`max_words=10000`) со спецкодами: `0` —
   паддинг, `1` — неизвестное слово (OOV), нумерация слов с `2`.
3. Преобразование текстов в последовательности индексов и выравнивание длины
   (`pad_sequences(maxlen=200)`).

**Часть Б. Классификация текстов.**

4. Собрать сеть со слоем `Embedding(max_words, 64)` и рекуррентным слоем
   `LSTM(64)` → `Dense(64, relu)` → `Dense(1, sigmoid)`. Обучить
   (`binary_crossentropy`, `Adam`), оценить точность.
5. Собрать альтернативу с `Conv1D(250, 5, relu)` → `GlobalMaxPooling1D` вместо
   LSTM. Сравнить точность и скорость обучения LSTM vs. 1D-CNN; сделать вывод.

**Часть В. Прогнозирование временного ряда.**

6. Загрузить `jena_climate_2009_2016.csv`, нормализовать признаки по train,
   сформировать окна с помощью `timeseries_dataset_from_array`
   (`sequence_length=120`, `sampling_rate=6`).
7. Построить и сравнить: простой бейзлайн («прогноз последним значением»),
   сеть `LSTM(16)` и `LSTM(32, recurrent_dropout=0.25)`. Оценить `MAE`,
   сравнить с бейзлайном.

## Формат сдачи

- Заполненный ноутбук с выполненными ячейками, графиками и сравнением
  архитектур.
- Вывод: в каких случаях LSTM предпочтительнее 1D-CNN (и наоборот); насколько
  рекуррентная сеть превосходит наивный бейзлайн на временном ряде.
- Устная защита.

## Критерии и шкала оценивания

Рубрика: [`rubric-01.md`](rubric-01.md).

## Контрольные вопросы для защиты

- Почему для текстов и временных рядов недостаточно полносвязной сети? В чём
  особенность данных переменной длины?
- Что делает слой `Embedding`? Откуда берутся его веса?
- Зачем `pad_sequences` и что значит `maxlen=200`? Что теряется при обрезке
  длинных текстов?
- Как работает обычная RNN? В чём её проблема затухающего градиента при длинных
  последовательностях? Как её решают LSTM и GRU?
- Что извлекает одномерная свёртка над эмбеддингами (n-граммы)? Зачем
  `GlobalMaxPooling1D`?
- Чем задача регрессии временного ряда отличается от классификации (функция
  потерь, выходной слой, метрики)?
- Почему нормализацию признаков временного ряда делают только по обучающей
  выборке? Зачем нужен наивный бейзлайн?

## Правила использования ресурсов и генеративного ИИ

- Допускается: документация Keras/nltk/pymorphy3, учебник d2l.ai (главы про RNN
  и LSTM), обсуждение концепций с LLM.
- Запрещается: просить LLM собрать рекуррентную сеть целиком.
- Сдача включает устную защиту с обоснованием выбора архитектуры.

## Замечание о материалах курса

В исходных ноутбуках `dlpython_course` GRU на текстах отсутствует (ноутбук
`07_cnn1d` номинально про GRU, но реализует 1D-CNN); GRU представлен только в
задаче временных рядов (`08_timeseries/weather_tensorflow.ipynb`, stacked GRU с
`recurrent_dropout`). Студент может опционально сравнить GRU с LSTM на временных
рядах для уровня С/П.

## Источники

- `dlpython_course/05_text_processing/text_classification.ipynb` (предобработка
  текста, словарь с кодами 0=pad/1=OOV).
- `dlpython_course/06_rnn/tensorflow/text_classification_lstm.ipynb`
  (`Embedding` + `LSTM`).
- `dlpython_course/07_cnn1d/tensorflow/text_classification_cnn1d.ipynb`
  (`Conv1D` + `GlobalMaxPooling1D`).
- `dlpython_course/08_timeseries/weather_tensorflow.ipynb` (Jena Climate,
  сравнение Dense/1D-CNN/LSTM/GRU/Bidirectional — образец структуры задания).
