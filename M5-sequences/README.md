# Модуль 5. Нейронные сети для анализа последовательностей

Раздел РПД №5. Нейронные сети для данных переменной длины — текстов и временных
рядов: одномерные свёрточные сети (1D-CNN), рекуррентные нейронные сети (RNN),
их улучшенные варианты LSTM и GRU, развёртывание рекуррентной сети во времени.
Применение к задачам классификации текстов и прогнозирования временных рядов.

## КИУ (КРМ v3.0)

| Компетенция | Индикатор | Уровень |
|---|---|:---:|
| DL-1. Архитектуры глубоких НС | DL-1.5 — спец. архитектуры для последовательностей | С |
| DL-4. Обработка естественного языка | DL-4.1 — алгоритмы и библиотеки NLP, предобученные модели | Б |

## КИМ модуля

| КИМ | Аннотация | Дескриптор | Формат |
|---|---|---|---|
| [КИМ 5.1. Рекуррентные сети и 1D-CNN для последовательностей](kim-01-sequences.md) | Единичная задержка и ручная развёртка RNN, классификация RNN/GRU/LSTM/1D-CNN, классический разбор текста, прогноз временного ряда; обязательная теория attention, опционально — практика с предобученным трансформером | **DL-1.5 (С):** понимает принцип работы блока единичной задержки в рекуррентных нейронных сетях; применяет блоки RNN, GRU, LSTM для решения задач. Наблюдаемые действия: ручная развёртка и общий текстовый split; сохраняется выбор `hidden_size=32/64`. **DL-4.1 (Б):** применяет regex, токенизацию, морфологический и синтаксический анализ. | Ноутбук + защита |

Построение словаря остаётся обязательным дополнительным наблюдаемым действием
КИМ. Оно выходит за предметный дескриптор DL-4.1 уровня Б, но само по себе не
означает подтверждение полного уровня С. Часть Г закрывает разрыв между аннотацией дисциплины («механизмы внимания») и
практикумом: теория scaled dot-product attention обязательна; сравнение с
DistilBERT/RuBERT остаётся опциональной практикой и не расширяет заявленный
уровень измерения.

## Протокол данных М5

Для классификации используется только `train.jsonl` Russian Reviews
Classification в revision `0f67d914f396ce22917dc6463ec619799b3b08d2` (SHA-256
`0b97698a0c6871437d17e07c973018af9b8c9230ec9048d85cb875cc2c2470ea`). После
удаления neutral при `seed=42` выбираются по 500 negative/positive и локально
делятся стратифицированно на train/validation/test 640/160/200. Локальный test
получен из опубликованного `train` и не является официальным test-сплитом;
полученные метрики нельзя заявлять как метрики official test набора.

Обязательная рекуррентная часть выполняется на **PyTorch**: `nn.RNNCell` и
`nn.RNN` используются для проверки единичной задержки, а `nn.RNN`, `nn.GRU` и
`nn.LSTM` — для классификации. NLP-инструменты (`nltk`, `pymorphy3`,
Natasha/spaCy/Stanza) не меняются. Тексты дополняются справа, реальные длины
передаются в рекуррентные модели через `pack_padded_sequence`. Test-метрики не
вычисляются до обучения 1D-CNN и фиксации общего победителя по validation.

## Связанные ресурсы

- [Датасеты](../resources/datasets/README.md) — Russian Reviews Classification,
  Jena Climate
- [Python-библиотеки](../resources/software/python-libs/README.md) — pymorphy3, nltk
- Исходные материалы курса: [`dlpython_course/05_text_processing/`](https://github.com/sozykin/dlpython_course), `06_rnn/`,
  `07_cnn1d/`, `08_timeseries/`
